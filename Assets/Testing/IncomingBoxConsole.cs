using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using TMPro;

public class IncomingBoxConsole : MonoBehaviour
{

   /* private void Start()
    {
        if (Connection.Instance != null)
            Connection.Instance.Mailbox = ReceiveData;
    }
    public void ReceiveData(byte[] data)
    {
        Debug.Log("Data: " + System.Text.Encoding.Default.GetString(data));
    }*/
}
